# Largest Element

## Problem
Find the largest element in an array.

## Approach
- Initialize `larg` with the first element.
- Traverse the array from index `1`.
- Update `larg` whenever a larger element is found.
- Return `larg`.

## Time Complexity
O(n)

## Space Complexity
O(1)